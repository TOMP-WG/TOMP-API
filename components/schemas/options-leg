    options-leg:
      type: object
      properties:
        start-time:
          $ref: '#/components/schemas/timestamp'
        end-time:
          $ref: '#/components/schemas/timestamp'
        from:
          $ref: '#/components/schemas/coordinates'
        to:
          $ref: '#/components/schemas/coordinates'

