    support-status:
      description: the current status of support
      type: object
      properties:
        status:
          type: string
          enum: [PROCESSING, UPDATEREQUESTED, RESOLVED, CANCELLED]
          example: PROCESSING
        time-to-resolution:
          type: integer
          description: time in minutes to expected resolution of support request
          example: 9
