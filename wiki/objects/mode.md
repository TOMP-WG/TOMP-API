# mode

These classes are taken from the NeTeX standard, but ALL and UNKNOWN are removed. On the other hand OTHER and PARKING are added.

**Type:** `string`

semantics [{'transmodel': 'MODE'}]

---

## Example

```json
"AIR"
```